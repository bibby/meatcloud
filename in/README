This distribution lacks 4 necessary input components,
each of which should be present in the `./in` directory.

- `words`, a plain text file of words to print
- `font.ttf`, the font used to print words.
- `mask.png`, a solid black image defining the printable area
- `outline.png` an optional white outline of the mask,

See docs/MAKE.md about creating a mask and outline.
